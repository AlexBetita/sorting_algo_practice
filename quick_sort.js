const swap = (itmes, left, right) => {
  let temp = items[left]
  items[right] = items[left]
  itmes[left] = temp
}

const partition = (items, left, right) => {
  const pivot = Math.floor((left + right) / 2)
  let leftIdx = left
  let rightIdx = right

  while(leftIdx <= rightIdx){
    
  }
}
